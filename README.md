# Distributed Task Queue

A highly reliable, production-grade distributed task queue built with Go and Redis Streams.

## Project Structure

- `api/`: API service for job submission and status checks.
- `worker/`: Distributed worker pool processing jobs.
- `tests/`: Integration and E2E tests.
- `.github/workflows/`: GitHub Actions CI pipeline.
